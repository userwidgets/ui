export const labels: Record<string, { name: string; hue: number; description: string } | undefined> = {
	"2fa": { name: "2fa", hue: 175, description: "User has two factor authentication enabled." },
}
